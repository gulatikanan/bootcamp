"""
Use Inline Comments Sparingly

Instructions:
Complete the exercise according to the requirements.
"""

def process_data(data):
    cleaned_data = [d.strip() for d in data]  # Strip whitespace
    return cleaned_data

def process_data(data):
    # Remove leading/trailing whitespace to clean the input
    cleaned_data = [d.strip() for d in data]
    return cleaned_data
